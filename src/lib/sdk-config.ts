import UniversalSDK from './sdk';

const githubUsername = import.meta.env.VITE_GITHUB_USERNAME || 'demo';
const githubRepo = import.meta.env.VITE_GITHUB_REPO || 'schoolportalbetadb';
const githubToken = import.meta.env.
